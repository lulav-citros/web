// import { add } from 'date-fns';
import _mock from '../_mock';
// ----------------------------------------------------------------------

export const _tags = [...Array(20)].map((_, index) => _mock.text.tag(index));
